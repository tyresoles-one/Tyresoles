{
  "query": "query GetDriveSyncAdminStatuses($userIds: [String!]!) { getDriveSyncAdminStatuses(userIds: $userIds, includeFolderValidation: true, includeUsage: true, includeLatestBackup: true) { userId } }",
  "variables": { "userIds": ["ACCOUNTS"] }
}
