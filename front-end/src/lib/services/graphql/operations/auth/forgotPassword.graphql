mutation ForgotPassword($username: String!, $securityPin: Int!, $newPassword: String!) {
  forgotPassword(username: $username, securityPin: $securityPin, newPassword: $newPassword) {
    success
    message
  }
}
