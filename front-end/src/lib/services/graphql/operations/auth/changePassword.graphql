mutation ChangePassword($userId: String!, $newPassword: String!, $oldPassword: String, $securityPin: Int) {
  changePassword(userId: $userId, newPassword: $newPassword, oldPassword: $oldPassword, securityPin: $securityPin) {
    success
    message
  }
}
